# Dependencias Python do BuscaAgil
# Instale com: pip install -r requirements.txt

Flask==3.1.2
python-dotenv==1.1.1
google-genai==2.17.0
pydantic==2.13.4
pandas==3.0.5
openpyxl==3.1.5
xlrd==2.0.2
python-docx==1.2.0
requests==2.34.2
beautifulsoup4==4.15.0
# transcrição/legenda de vídeos do YouTube sem precisar de API key (ver
# scripts/extrator_conteudo.py::_extrair_youtube) — pinado na 0.6.x porque
# a API usada aqui (YouTubeTranscriptApi.get_transcript classmethod) mudou
# na 1.x
youtube-transcript-api==0.6.3
celery==5.4.0
redis==5.0.8
filelock==3.16.1
rapidfuzz==3.14.1
google-api-python-client==2.198.0
google-auth==2.56.3
google-auth-oauthlib==1.2.2

# IA local (Ollama) - classificacao de PDF/video/audio sem depender do Gemini
pypdf==6.17.0
pypdfium2==5.13.0
Pillow==12.3.0
faster-whisper==1.2.1

# Para ler arquivos .doc legados, instale tambem no sistema:
# sudo apt-get install antiword

# Para extrair frames de video/audio na IA local, instale tambem no sistema:
# sudo apt-get install ffmpeg
